# ── IAP / Load Balancer ───────────────────────────────────────────────────────
# DISABLED for prototype — using NextAuth.js Google login in Next.js instead.
# Re-enable for production by restoring this file from git history and applying.
#
# Authentication for prototype: NextAuth.js (Google OAuth) in the portal app.
# Portal and ide-chat are publicly accessible via Cloud Run URLs.
# NextAuth.js protects all portal routes — unauthenticated users are redirected
# to Google login before they can access any page or API.
